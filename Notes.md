Improvements
1. Sometimes it confuses messages from different people as a conversation between those people (instead of separate conversations between the user and each of the participants).
2. Some answers with llama 3.1:8b are too literal ("What messages contain anything about Anthropic", shows the message titles, not the actual messages). Need to specifically indicate to the model to show the message contents or topics. 
3. Try with newer models (e.g. gemma4:e4b)

What works well:
1. source lists
2. seems to handle Greeklish fairly well (still need more messages to test).
3. good topic extraction in the limited data set I've used for testing. 